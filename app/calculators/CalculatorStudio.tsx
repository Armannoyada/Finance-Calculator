'use client';
import './calculator-studio.css';
import { useMemo, useState } from 'react';
import { ArrowLeft, Calculator, Landmark, TrendingUp, ReceiptText, Percent, PiggyBank, Wallet, Sparkles } from 'lucide-react';
import { addTax, calculateAmortization, calculateBreakEven, calculateCAGR, calculateCompound, calculateDebtPayoff, calculateDiscount, calculateEMI, calculateGoalSIP, calculateInflation, calculateLumpsum, calculateMargin, calculateMarkup, calculatePercentage, calculatePercentageChange, calculatePrepayment, calculateROI, calculateRule72, calculateSavingsGoal, calculateSimpleInterest, calculateSIP, calculateStepUpSIP, calculateSWP, calculateTip, removeTax } from '@/lib/calculations';
import { countries, taxData, CountryCode } from '@/lib/data';

export type Tool = 'EMI Calculator'|'Loan Payment'|'Amortization'|'Loan Comparison'|'Prepayment'|'SIP Calculator'|'Step-up SIP'|'Lumpsum'|'Compound Interest'|'Simple Interest'|'CAGR'|'SWP'|'SIP Goal'|'GST / VAT'|'Tax Inclusive / Exclusive'|'ROI'|'Profit Margin'|'Markup'|'Discount'|'Break-even'|'Savings Goal'|'Inflation'|'Debt Payoff'|'Percentage'|'Percentage Change'|'Tip Calculator'|'Rule of 72';
export const toolSlugs:Record<Tool,string>={
 'EMI Calculator':'emi-calculator','Loan Payment':'loan-payment','Amortization':'amortization-calculator','Loan Comparison':'loan-comparison','Prepayment':'loan-prepayment-calculator',
 'SIP Calculator':'sip-calculator','Step-up SIP':'step-up-sip-calculator','Lumpsum':'lumpsum-calculator','Compound Interest':'compound-interest-calculator','Simple Interest':'simple-interest-calculator','CAGR':'cagr-calculator','SWP':'swp-calculator','SIP Goal':'sip-goal-calculator',
 'GST / VAT':'gst-vat-calculator','Tax Inclusive / Exclusive':'tax-inclusive-exclusive-calculator','ROI':'roi-calculator','Profit Margin':'profit-margin-calculator','Markup':'markup-calculator','Discount':'discount-calculator','Break-even':'break-even-calculator','Savings Goal':'savings-goal-calculator','Inflation':'inflation-calculator','Debt Payoff':'debt-payoff-calculator','Percentage':'percentage-calculator','Percentage Change':'percentage-change-calculator','Tip Calculator':'tip-calculator','Rule of 72':'rule-of-72-calculator'
};
export const toolsBySlug=Object.fromEntries(Object.entries(toolSlugs).map(([tool,slug])=>[slug,tool])) as Record<string,Tool>;
const groups:{name:string;icon:typeof Landmark;tools:Tool[]}[]=[
 {name:'Loans',icon:Landmark,tools:['EMI Calculator','Loan Payment','Amortization','Loan Comparison','Prepayment']},
 {name:'Investments',icon:TrendingUp,tools:['SIP Calculator','Step-up SIP','Lumpsum','Compound Interest','Simple Interest','CAGR','SWP','SIP Goal']},
 {name:'Tax',icon:ReceiptText,tools:['GST / VAT','Tax Inclusive / Exclusive']},
 {name:'Business',icon:Percent,tools:['ROI','Profit Margin','Markup','Discount','Break-even']},
 {name:'Planning',icon:PiggyBank,tools:['Savings Goal','Inflation','Debt Payoff']},
 {name:'Everyday',icon:Wallet,tools:['Percentage','Percentage Change','Tip Calculator','Rule of 72']},
];
const defaults:Record<Tool,[number,number,number,number]>={
 'EMI Calculator':[1000000,8.5,20,0],'Loan Payment':[1000000,8.5,20,0],'Amortization':[1000000,8.5,20,0],'Loan Comparison':[1000000,8.5,20,0],'Prepayment':[1000000,8.5,20,10000],
 'SIP Calculator':[10000,12,10,0],'Step-up SIP':[10000,12,10,10],'Lumpsum':[100000,12,10,0],'Compound Interest':[100000,12,10,0],'Simple Interest':[100000,8,10,0],'CAGR':[100000,250000,5,0],'SWP':[1000000,15000,8,10],'SIP Goal':[2500000,12,10,0],
 'GST / VAT':[100000,18,0,0],'Tax Inclusive / Exclusive':[118000,18,0,0],'ROI':[100000,150000,0,0],'Profit Margin':[150000,100000,0,0],'Markup':[100000,150000,0,0],'Discount':[100000,10,0,0],'Break-even':[500000,2500,1500,0],
 'Savings Goal':[1000000,8,5,0],'Inflation':[100000,6,10,0],'Debt Payoff':[500000,12,15000,0],'Percentage':[50000,18,0,0],'Percentage Change':[100,125,0,0],'Tip Calculator':[2500,10,2,0],'Rule of 72':[8,0,0,0]
};
function money(value:number,currency:string){return new Intl.NumberFormat(undefined,{style:'currency',currency,maximumFractionDigits:0}).format(Number.isFinite(value)?value:0)}
const finite=(v:unknown)=>typeof v==='number'&&Number.isFinite(v)?v:0;
type Result={value?:number;total?:number;finalPrice?:number;net?:number;tax?:number;invested?:number;returns?:number;interest?:number;payment?:number;newMonths?:number;monthsSaved?:number;interestSaved?:number;units?:number;perPerson?:number;tip?:number;months?:number;paid?:number;balance?:number;totalWithdrawn?:number;depleted?:boolean};

export default function CalculatorStudio({initialTool='EMI Calculator'}:{initialTool?:Tool}){
 const[tool,setTool]=useState<Tool>(initialTool);const[country,setCountry]=useState<CountryCode>('IN');const[v,setV]=useState(defaults[initialTool]);const[taxMode,setTaxMode]=useState<'add'|'remove'>('add');
 const meta=countries.find(x=>x.code===country)!;const standardTax=taxData[country].standardIndirect;
 const choose=(next:Tool)=>{setTool(next);setV(next==='GST / VAT'?[100000,standardTax||18,0,0]:defaults[next])};
 const set=(i:number,raw:string)=>{const n=[...v] as typeof v;n[i]=Math.max(0,Number(raw)||0);setV(n)};
 const result=useMemo<Result>(()=>{const[a,b,c,d]=v;switch(tool){case'SIP Calculator':return calculateSIP(a,b,c);case'Step-up SIP':return calculateStepUpSIP(a,b,c,d);case'Lumpsum':return calculateLumpsum(a,b,c);case'Compound Interest':return calculateCompound(a,b,c);case'Simple Interest':return calculateSimpleInterest(a,b,c);case'CAGR':return{value:calculateCAGR(a,b,c)};case'SWP':return calculateSWP(a,b,c,d);case'SIP Goal':return{value:calculateGoalSIP(a,b,c)};case'GST / VAT':case'Tax Inclusive / Exclusive':return taxMode==='add'?addTax(a,b):removeTax(a,b);case'ROI':return{value:calculateROI(a,b)};case'Profit Margin':return{value:calculateMargin(a,b)};case'Markup':return{value:calculateMarkup(a,b)};case'Discount':return calculateDiscount(a,b);case'Break-even':return calculateBreakEven(a,b,c);case'Savings Goal':return{value:calculateSavingsGoal(a,0,b,c)};case'Inflation':return{value:calculateInflation(a,b,c)};case'Debt Payoff':return calculateDebtPayoff(a,b,c);case'Percentage':return{value:calculatePercentage(a,b)};case'Percentage Change':return{value:calculatePercentageChange(a,b)};case'Tip Calculator':return calculateTip(a,b,c);case'Rule of 72':return{value:calculateRule72(a)};case'Prepayment':return calculatePrepayment(a,b,c,d);case'Loan Comparison':{const base=calculateEMI(a,b,c),alt=calculateEMI(a,Math.max(0,b-1),c);return{payment:base,value:alt}}default:return{payment:calculateEMI(a,b,c),value:calculateEMI(a,b,c)}}},[tool,v,taxMode]);
 const percent=['CAGR','ROI','Profit Margin','Markup','Percentage Change'].includes(tool);const primary=result.value??result.payment??result.total??result.finalPrice??result.net??result.units??result.months??0;
 const labels:[string,string,string|null,string|null]=(()=>{switch(tool){case'CAGR':return['Initial value','Final value','Years',null];case'ROI':return['Investment / cost','Current value',null,null];case'Profit Margin':return['Revenue','Cost',null,null];case'Markup':return['Cost','Selling price',null,null];case'Break-even':return['Fixed costs','Price / unit','Variable cost / unit',null];case'Tip Calculator':return['Bill amount','Tip %','People',null];case'Rule of 72':return['Annual return %',null,null,null];case'Debt Payoff':return['Debt balance','Annual interest %','Monthly payment',null];case'Prepayment':return['Loan balance','Annual interest %','Loan years','Extra / month'];case'SIP Goal':return['Target amount','Expected return %','Years',null];case'SWP':return['Starting corpus','Monthly withdrawal','Annual return %','Years'];case'GST / VAT':case'Tax Inclusive / Exclusive':return['Amount','Tax rate %',null,null];case'Discount':return['Original price','Discount %',null,null];case'Inflation':return['Current amount','Inflation %','Years',null];case'Percentage':return['Number','Percentage %',null,null];case'Percentage Change':return['Original value','New value',null,null];case'Step-up SIP':return['Monthly investment','Expected return %','Years','Annual step-up %'];default:return['Principal / amount','Annual rate %','Years',null]}})();
 const showTax=tool==='GST / VAT'||tool==='Tax Inclusive / Exclusive';
 return <main className="calculator-page"><header className="calculator-header"><a href="/Finance-Calculator/" className="back"><ArrowLeft size={17}/> FinCalc</a><div className="studio-title"><Calculator size={17}/> Calculator Studio</div><div className="studio-badge"><Sparkles size={14}/> {groups.reduce((n,g)=>n+g.tools.length,0)} tools</div></header><div className="calculator-layout"><aside className="calculator-sidebar"><div className="side-title"><Calculator size={18}/> Tools</div>{groups.map(g=>{const Icon=g.icon;return <div key={g.name} className="tool-group"><div className="group-name"><Icon size={15}/>{g.name}</div>{g.tools.map(item=><button key={item} className={tool===item?'tool-select active':'tool-select'} onClick={()=>choose(item)}>{item}</button>)}</div>})}</aside><section className="calculator-workspace"><div className="workspace-top"><div><span className="eyebrow">CALCULATOR</span><h1>{tool}</h1><p>Model a real scenario with clear assumptions and instant results.</p></div><select value={country} onChange={e=>setCountry(e.target.value as CountryCode)} aria-label="Country and currency">{countries.map(item=><option key={item.code} value={item.code}>{item.flag} {item.name} — {item.currency}</option>)}</select></div><div className="calculator-card">{showTax&&<div className="mode-toggle"><button className={taxMode==='add'?'selected':''} onClick={()=>setTaxMode('add')}>Add tax</button><button className={taxMode==='remove'?'selected':''} onClick={()=>setTaxMode('remove')}>Remove tax</button><span>Reference rate: {standardTax||'—'}%</span></div>}<div className="inputs">{[0,1,2,3].map(i=>labels[i]&&<label key={i}>{labels[i]}<input type="number" min="0" step="0.01" value={v[i]} onChange={e=>set(i,e.target.value)}/></label>)}</div><div className="primary-result"><span>ESTIMATED RESULT</span><strong>{percent?`${finite(primary).toFixed(2)}%`:tool==='Rule of 72'?`${finite(primary).toFixed(1)} years`:tool==='Break-even'?`${Number.isFinite(primary)?finite(primary).toFixed(0):'—'} units`:tool==='Debt Payoff'?`${Number.isFinite(primary)?finite(primary).toFixed(0):'—'} months`:money(finite(primary),meta.currency)}</strong>{result.invested!==undefined&&<div className="result-mini"><span>Invested {money(finite(result.invested),meta.currency)}</span><span>Returns {money(finite(result.returns),meta.currency)}</span></div>}{result.tax!==undefined&&<div className="result-mini"><span>Tax {money(result.tax,meta.currency)}</span><span>{taxMode==='add'?'Total':'Net'} {money(finite(taxMode==='add'?result.total:result.net),meta.currency)}</span></div>}{tool==='Prepayment'&&<div className="result-mini"><span>Months saved {result.monthsSaved??0}</span><span>Interest saved {money(finite(result.interestSaved),meta.currency)}</span></div>}{tool==='Tip Calculator'&&<div className="result-mini"><span>Tip {money(finite(result.tip),meta.currency)}</span><span>Per person {money(finite(result.perPerson),meta.currency)}</span></div>}{tool==='SWP'&&<div className="result-mini"><span>Total withdrawn {money(finite(result.totalWithdrawn),meta.currency)}</span><span>{result.depleted?'Corpus depleted':'Corpus remaining'}</span></div>}{tool==='Loan Comparison'&&<div className="result-mini"><span>Base EMI {money(finite(result.payment),meta.currency)}</span><span>At 1% lower rate {money(finite(result.value),meta.currency)}</span></div>}</div>{tool==='Amortization'&&<div className="table-wrap"><table><thead><tr><th>Month</th><th>Payment</th><th>Principal</th><th>Interest</th><th>Balance</th></tr></thead><tbody>{calculateAmortization(v[0],v[1],v[2]).rows.slice(0,12).map(row=><tr key={row.month}><td>{row.month}</td><td>{money(row.payment,meta.currency)}</td><td>{money(row.principal,meta.currency)}</td><td>{money(row.interest,meta.currency)}</td><td>{money(row.balance,meta.currency)}</td></tr>)}</tbody></table></div>}{tool==='Amortization'&&<div className="table-note">Showing the first 12 months. Total scheduled interest: {money(calculateAmortization(v[0],v[1],v[2]).totalInterest,meta.currency)}.</div>}<div className="assumptions"><b>About this calculation</b><p>FinCalc uses transparent mathematical models. Tax figures are reference defaults, not tax advice, and can vary by jurisdiction, filing year, taxpayer status, exemptions and transaction type.</p></div></div></section></div></main>;
}
